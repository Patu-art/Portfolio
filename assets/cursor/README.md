# Custom cursor
`cursor-default.svg` = normal desktop cursor visual.
`cursor-hover.svg` = hover state over interactive elements.
Replace either file with your own SVG/PNG and keep the same filename, or update `js/cursor.js`.
Cursor is automatically disabled on touch/coarse-pointer devices and when reduced motion is preferred.
